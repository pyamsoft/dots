#!/bin/sh

_path=""
if [ "$1" = "-2" ]; then
  _path="media2"
  shift
else
  _path="media"
fi

# Remove a trailing space to copy over folders
_file="${1%/}"

exec rsync -hrtplu --progress "${_file}" "nerd@nerd2:/home/nerd/${_path}/Movies"
