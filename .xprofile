# shellcheck shell=bash
#
# Source xprofile

# This used to launch start up programs, but now
# we do so using the systemd user unit login-autostart
# which is display backend agnostic

# Source our environment file
# shellcheck disable=SC1091
[ -f "${HOME}/.environment" ] && . "${HOME}/.environment"
