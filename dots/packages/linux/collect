#!/bin/sh

main() {
  pacman -Qneq >./repo || return 1
  pacman -Qndq >./repo-deps || return 1

  pacman -Qmeq >./aur || return 1
  pacman -Qmdq >./aur-deps || return 1
  flatpak list --app --columns=application >./flatpak || return 1
  return 0
}

main "$@" || exit 1
exit 0
