[Service]
# We are running in remote mode
Environment=BESZEL_MODE=remote

# Run beszel
ExecStartPre=
ExecStartPre=/bin/test -x /opt/pyamsoft/containers/bin/beszel

ExecStart=
ExecStart=/opt/pyamsoft/containers/bin/beszel
