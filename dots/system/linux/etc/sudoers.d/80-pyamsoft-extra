##########
# Safe access to launching the gamelaunch script
# <<PROGRAM>> is the direct name of a program, like "steam" which
# should have a corresponding "gaming-steam" user on the system
%gamelaunch ALL=(root) NOPASSWD:/opt/pyamsoft/sbin/gamelaunch <<PROGRAM>
# Keep gamer related vars only for gamelaunch script
Defaults!/opt/pyamsoft/sbin/gamelaunch env_keep += "GAMER_*"

##########
# Allow user to check tailscale status
%tailscale ALL=(root) NOPASSWD:/opt/pyamsoft/bin/tailscale ""
%tailscale ALL=(root) NOPASSWD:/opt/pyamsoft/bin/tailscale status
%tailscale ALL=(root) NOPASSWD:/opt/pyamsoft/bin/tailscale version
%tailscale ALL=(root) NOPASSWD:/opt/pyamsoft/bin/tailscale ip

##########
# Run "podman ps" without password
%podman ALL=(root) NOPASSWD:/usr/bin/podman ps

##########
# Run "cscli lapi status" without password
%crowdsec ALL=(root) NOPASSWD:/opt/pyamsoft/bin/cscli lapi status
