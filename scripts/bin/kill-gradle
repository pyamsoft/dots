#!/bin/sh

gradle_pid="$(ps aux | grep -v 'grep' | grep -E "GradleDaemon" | awk '{ print $2 }')"
if [ -n "${gradle_pid}" ]; then
  printf -- 'Killing Gradle Daemon process %s\n' "${gradle_pid}"
  kill "${gradle_pid}"
else
  printf -- 'Gradle Daemon not running.\n'
fi
