#!/bin/sh

landscape="DisplayPort-1"
portrait="HDMI-A-0"

exec xrandr \
  --output "${landscape}" --auto --pos 0x0 --primary \
  --output "${portrait}" --auto -s 1920x1080 --rotate right --pos 0x0 --right-of "${landscape}"

