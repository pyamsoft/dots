#!/bin/sh

# Sometimes PulseAudio gets set to use a profile that, while available,
# is not plugged in on this system.
# Fix it back to a working, plugged in profile

pactl set-card-profile 0 output:hdmi-stereo-extra3
pactl set-card-profile 1 output:analog-stereo+input:analog-stereo
