#!/bin/sh

readonly pak="org.gnome.Lollypop"

self="$(id -un)"
readonly self

cmd=""
if [ "$1" = "--shell" ]; then
  cmd="${cmd} --shell"
  shift
fi
readonly cmd

# shellcheck disable=SC2086
exec flatpak-env ${cmd} "${pak}" \
  --filesystem="/storage/${self}/music" \
  "$@" || exit 1
exit 0
