#!/bin/sh

readonly pak="com.jetbrains.WebStorm"

self="$(id -un)"
readonly self

cmd=""
if [ "$1" = "--shell" ]; then
  cmd="${cmd} --shell"
  shift
fi
readonly cmd

# shellcheck disable=SC2086
exec flatpak-env ${cmd} "${pak}" \
  --filesystem="/storage/${self}/devel/js" \
  "$@" || exit 1
exit 0
