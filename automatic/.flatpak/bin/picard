#!/bin/sh

delete_old()
{
  rm -f "$1" || {
    printf -- 'Could not delete old symlink at %s\n' "$1"
    return 1
  }

  return 0
}

delete_old "${HOME}/.config/MusicBrainz" || exit 1

exec flatpak run org.musicbrainz.Picard "$@"
