# shellcheck shell=bash

# Strict umask
if command -v umask >/dev/null; then
  umask 077 || return 1

  # Set up vars
  if [ -z "${PYAMSOFT_MACHINE}" ]; then
    PYAMSOFT_MACHINE="umask"
  else
    PYAMSOFT_MACHINE="umask:${PYAMSOFT_MACHINE}"
  fi
fi

return 0
