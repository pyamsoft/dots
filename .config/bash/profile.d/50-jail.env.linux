# shellcheck shell=bash

# Jail scrips
if command -v jail >/dev/null; then
  if [ -d "/opt/pyamsoft/jail/bin" ]; then
    PATH="/opt/pyamsoft/jail/bin:${PATH}"
  fi
fi

# Export the PATH
export PATH

return 0

# vim: set filetype=sh:
