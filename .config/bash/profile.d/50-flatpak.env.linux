# shellcheck shell=bash

# Flatpak scrips
if command -v flatpak >/dev/null; then
  if [ -d "/opt/pyamsoft/flatpak/bin" ]; then
    PATH="/opt/pyamsoft/flatpak/bin:${PATH}"
  fi
fi

# Export the PATH
export PATH

return 0

# vim: set filetype=sh:
