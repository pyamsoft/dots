# shellcheck shell=bash

alias grep="grep -E --color=auto "

# Quick ls
alias ll="ls -l"
alias la="ls -A"
alias lla="ls -Al"
alias lal="ls -Al"

# Verbose
alias rm="rm -iv"
alias cp="cp -iv"
alias mv="mv -iv"
alias mkdir="mkdir -vp"

# Cleaner human output
alias df="df -H"
alias du="du -ch"
