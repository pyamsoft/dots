[Unit]
Description=On %u login, start container %i

[Service]
ExecStart=/bin/sh -c 'podman stop %i; podman rm %i; %h/.containers/bin/%i'
ExecStop=/bin/sh -c 'podman stop %i && podman rm %i || exit 0'

[Install]
WantedBy=default.target
